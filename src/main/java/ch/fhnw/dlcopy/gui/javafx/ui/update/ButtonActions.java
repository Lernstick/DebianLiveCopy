/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Class.java to edit this template
 */
package ch.fhnw.dlcopy.gui.javafx.ui.update;


/*import ch.fhnw.dlcopy.DLCopy;
import static ch.fhnw.dlcopy.DLCopy.MEGA;
import javafx.application.Application;
import javafx.geometry.Pos;
import javafx.scene.Scene;
import javafx.scene.control.Button;
import javafx.scene.layout.VBox;
import javafx.scene.control.Label;
import javafx.stage.Modality;
import javafx.stage.Stage;
import ch.fhnw.dlcopy.gui.javafx.ui.View;
/**
 *
 * @author user
 
public class ButtonActions extends View{
    
    
    public static void display(String title, String message){
        
        Stage window = new Stage();
        window.initModality(Modality.APPLICATION_MODAL);
        window.setTitle(title);
        window.setMinWidth(250);
        Label label = new Label();
        label.setText(message);
        
        //creating the options for the buttons
        Button abort = new Button(stringBundle.getString("error.abort"));
        Button revert = new Button(stringBundle.getString("error.revert"));
        Button goBack = new Button(stringBundle.getString("error.goBack"));
        
        abort.setOnAction(event -> {
            window.close();
        });
        
       revert.setOnAction(event ->{
           window.close();
       });
       
       goBack.setOnAction(event ->{
           window.close();
       });
        
        VBox layout = new VBox(10);
        layout.getChildren().addAll(label);
        layout.setAlignment(Pos.CENTER);
        Scene scene = new Scene(layout);
        window.setScene(scene);
        window.showAndWait();
        
    }    
}*/


